<Window x:Class="WhackerLinkConsoleV2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WhackerLinkConsoleV2" Height="600" Width="1000" Background="#FFF2F2F2">
    <Grid Background="#FFF2F2F2">
        <!-- Column Definitions -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="497*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Row Definitions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="52"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Menu Background -->
        <Border BorderBrush="#FFB7B7B7" BorderThickness="1" Grid.Row="1" Grid.ColumnSpan="2">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="White"/>
                    <GradientStop Color="#FFDFDFDF" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Menu -->
        <Menu VerticalAlignment="Center" Height="25" Background="White" Grid.ColumnSpan="2">
            <MenuItem Header="File">
                <MenuItem Header="Open Codeplug" Click="OpenCodeplug_Click"/>
                <!-- <MenuItem Header="Open Debug Console" Click="OpenDebugConsole_Click" /> -->
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Audio Settings" Click="AudioSettings_Click"/>
                <MenuItem Header="Enable Edit Mode" Click="ToggleEditMode_Click"/>
                <MenuItem Header="Select Widgets to Display" Click="SelectWidgets_Click"/>
                <MenuItem Header="Reset Settings" Click="ResetSettings_Click"/>
                <MenuItem Header="Alerts">
                    <MenuItem Header="Add Alert Tone" Click="AddAlertTone_Click"/>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="Page">
                <MenuItem Header="P25 Page" Click="P25Page_Click"/>
                <MenuItem Header="Manual QC2" Click="ManualPage_Click"/>
            </MenuItem>
        </Menu>

        <!-- Zones Tabs -->
        <TabControl x:Name="ChannelsTabControl"
                    Background="#FFF2F2F2"
                    Grid.Row="2"
                    Grid.ColumnSpan="2"
                    Panel.ZIndex="1">
            <!-- Default Tab containing ChannelsCanvas -->
            <TabItem Header="SYSTEMS" Height="100" Width="100">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Canvas x:Name="ChannelsCanvas" Background="Transparent"/>
                </ScrollViewer>
            </TabItem>

            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Scrollable Tab Headers -->
                        <ScrollViewer Grid.Row="0"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Disabled"
                                      Focusable="False"
                                      CanContentScroll="False">
                            <TabPanel x:Name="HeaderPanel"
                                      IsItemsHost="True"
                                      Panel.ZIndex="1"
                                      Background="{TemplateBinding Background}"/>
                        </ScrollViewer>

                        <!-- Selected Tab Content -->
                        <Border Grid.Row="1"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Margin="{TemplateBinding Padding}"
                                              ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>
        </TabControl>

        <!-- Logo -->
        <Image HorizontalAlignment="Left" Height="50" Grid.Row="1" VerticalAlignment="Center" Width="231" Source="/Assets/WhackerLinkLogoV4.png" Stretch="UniformToFill" Margin="5,0,0,0">
            <Image.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleY="-0.164"/>
                    <RotateTransform/>
                    <TranslateTransform Y="-0.247"/>
                </TransformGroup>
            </Image.RenderTransform>
        </Image>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="238,0,0,0" Grid.Row="1">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Height" Value="46"/>
                    <Setter Property="Width" Value="45"/>
                    <Setter Property="FontSize" Value="10"/>
                    <Setter Property="FontFamily" Value="Arial"/>
                    <Setter Property="BorderBrush" Value="#FFC1C1C1"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                <GradientStop Color="#FFF0F0F0" Offset="0.485"/>
                                <GradientStop Color="#FFDBDBDB" Offset="0.517"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Style>
            </StackPanel.Resources>
            <Button x:Name="OpenCodeplug_Click2" Click="OpenCodeplug_Click">
                <Image Source="pack://application:,,,/WhackerLinkConsoleV2;component/Assets/config2.png" Width="36" Height="40"/>
            </Button>

            <Button x:Name="btnAlert1" Click="btnAlert1_Click">
                <Image HorizontalAlignment="Left" Grid.Row="1" Width="41" Source="/Assets/instantptt.png" OpacityMask="Black"/>
            </Button>

            <Button x:Name="btnAlert2" Click="btnAlert2_Click">
                <Image HorizontalAlignment="Left" Grid.Row="1" Width="41" Source="/Assets/alerttone2.png" OpacityMask="Black"/>
            </Button>

            <Button x:Name="btnAlert3" Click="btnAlert3_Click">
                <Image HorizontalAlignment="Left" Grid.Row="1" Width="41" Source="/Assets/alerttone2.png" OpacityMask="Black"/>
            </Button>

            <Button x:Name="btnGlobalPtt" Click="btnGlobalPtt_Click">
                <Image HorizontalAlignment="Left" Grid.Row="1" Width="41" Source="/Assets/alerttone2.png" OpacityMask="Black"/>
            </Button>

            <Button Click="P25Page_Click">
                <Image HorizontalAlignment="Left" Grid.Row="1" Width="41" Source="/Assets/page.png" OpacityMask="Black"/>
            </Button>

            <Button Click="AudioSettings_Click">
                <Image HorizontalAlignment="Left" Grid.Row="1" Width="41" Source="/Assets/audio.png" OpacityMask="Black"/>
            </Button>

            <Button Click="ClearEmergency_Click">
                <Image HorizontalAlignment="Left" Grid.Row="1" Width="41" Source="/Assets/clearemerg.png" OpacityMask="Black"/>
            </Button>


            <Button Content="Select All" Click="SelectAll_Click"/>
            <Button Content="Key Stats" Click="KeyStatus_Click"/>
            <Button Content="Call Hist" Click="CallHist_Click"/>
        </StackPanel>
    </Grid>
</Window>
